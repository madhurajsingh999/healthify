server:
  port: 8888

spring:
  application:
    name: healthify-config
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          searchLocations: file:C:/Users/Admin/OneDrive/Documents/Healthify/healthify/healthify-ms-config
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8061/eureka/
---

